# ── Backend host ──────────────────────────────────────────────────────────────
# BACKEND_HOST options:
#   localhost    → backend on the same machine
#   10.0.2.6     → backend on a remote VM (Azure / AWS private subnet)
#   crud-backend → backend container on the same Docker network
BACKEND_HOST=10.0.2.218
